{
  "name": "@pidetucoche/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": { ".": "./dist/index.js" },
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "migrate": "node scripts/migrate.mjs",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@pidetucoche/domain": "workspace:*",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/pg": "^8.15.5"
  }
}
